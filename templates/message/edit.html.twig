{% extends 'base.html.twig' %}

{% block title %}Nouveau message{% endblock %}

{% block body %}
   
    {{form_start(form, {'attr': {'class' :'text-center border border-light p-5 mx-5', 'style': 'box-shadow: 1px 1px 15px black;'}})}}

        <!-- Default form subscription -->


    <p class="h4 mb-4 text-default">Votre message</p>

    <!-- Name -->
    {#<div class="md-form">
    {{ form_widget(form.sender)}}
    {{ form_label(form.sender)}}
    </div>

    <div class="md-form">
    {{ form_label(form.receiver)}}
    {{ form_widget(form.receiver)}}
    </div>#}

    <div class="md-form">
    {{ form_label(form.body)}}
    {{ form_widget(form.body)}}
    </div>

   
    
    {{ form_end(form) }} 

   <a href="{{ path('message_index') }}" class="default-text">Retour aux messages</a>  


   {% endblock %}
